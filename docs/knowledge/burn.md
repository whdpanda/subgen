# Burn Subtitles (ffmpeg)

- implemented in `subgen.core.video.burn`
- uses ffmpeg `subtitles` filter
- supports `force_style` (ASS style string)
- audio defaults to AAC 192k (configurable)

Tool:
- `burn_subtitles` wraps `burn_subtitles(...)`
